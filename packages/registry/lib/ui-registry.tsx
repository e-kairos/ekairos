"use client";

import { useContextRegistryItem } from "@/app/docs/components/use-context/config";
import type { RegistryItem } from "@/lib/registry-types";

export const registryData: RegistryItem[] = [useContextRegistryItem];
