diff --git a/packages/workbench-v3/README.md b/packages/workbench-v3/README.md
index c28dfcf..cea2572 100644
--- a/packages/workbench-v3/README.md
+++ b/packages/workbench-v3/README.md
@@ -14,12 +14,14 @@ while historical streams are not replayed until selected.
 
 The default `Causal review` scenario proves the full path:
 
-1. Dataset materialization, requirements, and risks execute in parallel.
-2. A decision Reaction joins their effect Events.
-3. `workbench.recordReview` validates `dataset(workbenchItemSchema)` and writes
+1. Git clone and event-file materialization execute in parallel.
+2. Dataset materialization and structured analysis execute in parallel.
+3. A repository-aware decision Reaction joins those effect Events.
+4. Shell writes the report, `storeFiles` publishes it, and Git commits it.
+5. `workbench.recordReview` validates `dataset(workbenchItemSchema)` and writes
    the linked durable review.
-4. `workbench.reviewCompleted` is emitted as the final domain Event.
-5. Dataset's own materialization appears as a nested child Session.
+6. `workbench.reviewCompleted` links the review and artifact as the final domain
+   Event; Dataset materialization remains visible as a nested child Session.
 
 At the beginning of a causal review, the Reaction sandbox resolver probes the
 cached Daytona lease. A missing remote sandbox is replaced once under a shared
diff --git a/packages/workbench-v3/app/api/context/route.ts b/packages/workbench-v3/app/api/context/route.ts
index 980c5a9..30bacf2 100644
--- a/packages/workbench-v3/app/api/context/route.ts
+++ b/packages/workbench-v3/app/api/context/route.ts
@@ -4,7 +4,7 @@ import { answerMessage } from "@/src/chat-reaction"
 import { workbenchScenarioSchema } from "@/src/domain"
 import { ensureWorkbenchContext } from "@/src/runtime"
 
-export const maxDuration = 120
+export const maxDuration = 300
 
 function text(value: unknown) {
   return typeof value === "string" ? value.trim() : ""
@@ -47,6 +47,7 @@ export async function POST(request: Request) {
       event,
     })
   } catch (error) {
+    console.error("[workbench-v3] Reaction request failed", error)
     return NextResponse.json(
       { error: error instanceof Error ? error.message : String(error) },
       { status: 500 },
diff --git a/packages/workbench-v3/app/globals.css b/packages/workbench-v3/app/globals.css
index d5eb1ca..96b5218 100644
--- a/packages/workbench-v3/app/globals.css
+++ b/packages/workbench-v3/app/globals.css
@@ -70,6 +70,7 @@ code, pre { font-family: "Cascadia Code", "SFMono-Regular", Consolas, monospace;
 .message header strong { color: #dce0df; font-size: 11px; }
 .message-user .message-author { color: var(--amber); }
 .message-assistant .message-author { color: var(--green); }
+.message-files { display: inline-flex; align-items: center; gap: 3px; color: var(--cyan); }
 .durability { margin-left: auto; padding: 2px 5px; border: 1px solid var(--line); font-size: 8px; text-transform: uppercase; }
 .durability-streaming { color: var(--cyan); border-color: #246168; }
 .durability-optimistic { color: var(--amber); border-color: #68542d; }
@@ -90,21 +91,56 @@ code, pre { font-family: "Cascadia Code", "SFMono-Regular", Consolas, monospace;
 .chat-empty span { max-width: 340px; font-size: 11px; line-height: 1.5; }
 .error-banner { display: flex; align-items: center; gap: 8px; padding: 8px 12px; border-top: 1px solid #6e302d; background: #2a1515; color: #ffc3be; font-size: 10px; }
 .composer-shell { margin: 0 clamp(12px, 3vw, 32px) 14px; border: 1px solid #3b4042; background: #17191a; box-shadow: 0 8px 28px #0008; }
-.scenario-bar { min-height: 34px; display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 4px 6px; border-bottom: 1px solid var(--line-soft); }
+.scenario-bar { min-height: 34px; display: flex; align-items: center; gap: 8px; padding: 4px 6px; border-bottom: 1px solid var(--line-soft); }
 .scenario-control { display: inline-flex; align-items: center; border: 1px solid #34393a; background: #101213; }
 .scenario-control button, .load-sample { min-height: 24px; display: inline-flex; align-items: center; gap: 5px; padding: 0 8px; border: 0; background: transparent; color: var(--muted); cursor: pointer; font-size: 9px; }
 .scenario-control button + button { border-left: 1px solid #34393a; }
 .scenario-control button.active { background: #213229; color: #c9f4d5; }
 .load-sample { color: var(--cyan); }
 .load-sample:hover { background: #182226; }
-.composer { display: grid; grid-template-columns: minmax(0, 1fr) 38px; align-items: end; gap: 8px; padding: 8px; }
+.attachment-chip { min-width: 0; margin-left: auto; display: inline-flex; align-items: center; gap: 5px; padding: 3px 5px; border: 1px solid #2e5559; color: #a7dfe3; background: #122024; font-size: 8px; }
+.attachment-chip > span { max-width: 130px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
+.attachment-chip button { width: 18px; height: 18px; display: grid; place-items: center; padding: 0; border: 0; color: #a7dfe3; background: transparent; cursor: pointer; }
+.composer { display: grid; grid-template-columns: 36px minmax(0, 1fr) 38px; align-items: end; gap: 8px; padding: 8px; }
 .composer textarea { width: 100%; min-height: 40px; max-height: 130px; resize: vertical; border: 0; outline: 0; padding: 9px; background: transparent; color: var(--text); font-size: 12px; line-height: 1.45; }
 .composer textarea::placeholder { color: #737a79; }
-.composer button { width: 36px; height: 36px; border: 1px solid #4b7b5a; background: #21432c; color: #c8fbd6; display: grid; place-items: center; cursor: pointer; }
+.composer button { width: 36px; height: 36px; display: grid; place-items: center; cursor: pointer; }
+.composer .attach-button { border: 1px solid #3a4143; background: #151819; color: #a8afae; }
+.composer .attach-button:hover { border-color: #4a6b70; color: var(--cyan); }
+.composer .send-button { border: 1px solid #4b7b5a; background: #21432c; color: #c8fbd6; }
 .composer button:disabled { cursor: not-allowed; opacity: .4; }
 
-.traces { display: grid; grid-template-rows: 39px minmax(210px, 46%) minmax(0, 1fr); overflow: hidden; background: #101213; }
+.traces { display: grid; grid-template-rows: 39px minmax(280px, 55%) minmax(0, 1fr); overflow: hidden; background: #101213; }
 .reaction-flow { overflow: auto; border-bottom: 1px solid var(--line); background: #0e1011; }
+.event-graph { height: 100%; min-width: 0; display: grid; grid-template-rows: 42px minmax(0, 1fr); }
+.event-graph-toolbar { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 6px 8px; border-bottom: 1px solid var(--line-soft); background: #121516; }
+.event-graph-toolbar > div:first-child { min-width: 0; display: grid; gap: 2px; }
+.event-graph-toolbar strong { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; color: #dde2e0; font-size: 9px; }
+.event-graph-toolbar span { color: var(--muted); font-size: 7px; }
+.graph-zoom { flex: 0 0 auto; display: grid; grid-template-columns: 24px 34px 24px; align-items: center; border: 1px solid #303537; background: #0d0f10; }
+.graph-zoom button { width: 24px; height: 23px; display: grid; place-items: center; padding: 0; border: 0; background: transparent; color: #9da5a3; cursor: pointer; }
+.graph-zoom button:hover { color: var(--cyan); background: #172022; }
+.graph-zoom span { text-align: center; color: #aeb5b3; font: 7px/1 monospace; }
+.event-graph-viewport { min-height: 0; overflow: auto; background-color: #0b0d0e; background-image: linear-gradient(#202426 1px, transparent 1px), linear-gradient(90deg, #202426 1px, transparent 1px); background-size: 20px 20px; }
+.event-graph-scaled { position: relative; margin: 12px auto 30px; }
+.event-graph-canvas { position: absolute; inset: 0 auto auto 0; transform-origin: top left; }
+.event-graph-canvas > svg { position: absolute; inset: 0; z-index: 0; overflow: visible; pointer-events: none; }
+.event-graph-canvas > svg path { fill: none; stroke: #4b5557; stroke-width: 1.35; }
+.event-graph-canvas > svg path.selected { stroke: var(--cyan); stroke-width: 2; }
+.event-graph-canvas > svg marker path { fill: #687274; stroke: none; }
+.event-node { position: absolute; z-index: 2; min-width: 0; display: grid; grid-template-rows: auto minmax(0, 1fr) auto; gap: 5px; padding: 8px; overflow: hidden; border: 1px solid #3a4143; border-radius: 3px; background: #15191a; color: #dce1df; text-align: left; box-shadow: 0 5px 14px #0008; cursor: pointer; }
+.event-node:hover { border-color: #617073; background: #192021; }
+.event-node.durability-streaming { border-color: #327680; box-shadow: 0 0 0 1px #46c9d42b, 0 5px 14px #0008; }
+.event-node.durability-optimistic { border-color: #725f37; }
+.event-node-type { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; color: #86d5dc; font: 700 8px/1.2 monospace; }
+.event-node > strong { overflow: hidden; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; color: #e0e5e3; font-size: 9px; font-weight: 600; line-height: 1.35; }
+.event-node-meta { display: flex; align-items: center; gap: 6px; min-width: 0; color: #7f8987; font-size: 7px; }
+.event-node-meta code { margin-right: auto; overflow: hidden; text-overflow: ellipsis; }
+.reaction-edge { position: absolute; z-index: 3; min-width: 0; display: grid; grid-template-columns: auto minmax(0, 1fr) auto; align-items: center; gap: 5px; padding: 0 7px; overflow: hidden; border: 1px solid #4b5355; border-radius: 3px; background: #202426; color: #d6dcda; box-shadow: 0 3px 9px #000a; cursor: pointer; }
+.reaction-edge strong { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; font: 700 7px/1 monospace; text-transform: uppercase; }
+.reaction-edge > span:last-child { color: #8d9694; font: 7px/1 monospace; }
+.reaction-edge:hover, .reaction-edge.selected { border-color: var(--cyan); background: #172a2d; }
+.reaction-edge.reaction-failed { border-color: #8d4642; }
 .session-flow { padding: 9px; }
 .session-flow.nested { margin-top: 10px; padding: 8px; border-left: 2px solid #3b545a; background: #111619; }
 .session-flow-header { display: grid; grid-template-columns: auto minmax(0, 1fr) auto; align-items: center; gap: 8px; padding: 0 2px 8px; }
diff --git a/packages/workbench-v3/next.config.ts b/packages/workbench-v3/next.config.ts
index 35bc2d2..059b0bc 100644
--- a/packages/workbench-v3/next.config.ts
+++ b/packages/workbench-v3/next.config.ts
@@ -1,12 +1,24 @@
 import type { NextConfig } from "next"
 
 const nextConfig: NextConfig = {
+  serverExternalPackages: [
+    "@ekairos/sandbox",
+    "@mongodb-js/zstd",
+    "just-bash",
+    "node-liblzma",
+  ],
   transpilePackages: [
     "@ekairos/domain",
     "@ekairos/events",
     "@ekairos/reactor",
     "@ekairos/testing",
   ],
+  webpack(config, { webpack }) {
+    config.plugins.push(new webpack.IgnorePlugin({
+      resourceRegExp: /^(@mongodb-js\/zstd|node-liblzma)$/,
+    }))
+    return config
+  },
 }
 
 export default nextConfig
diff --git a/packages/workbench-v3/src/chat-reaction.ts b/packages/workbench-v3/src/chat-reaction.ts
index 15a07a8..ea13180 100644
--- a/packages/workbench-v3/src/chat-reaction.ts
+++ b/packages/workbench-v3/src/chat-reaction.ts
@@ -15,11 +15,10 @@ import { executeReaction } from "@ekairos/reactor/internal"
 
 import { getAzureModel } from "./azure"
 import {
+  workbenchAnalysisSchema,
   workbenchDecisionSchema,
   workbenchDomain,
   workbenchItemSchema,
-  workbenchRequirementsSchema,
-  workbenchRisksSchema,
   type WorkbenchScenario,
 } from "./domain"
 import {
@@ -66,48 +65,92 @@ function createReviewReaction(resolveSandbox: () => Promise<string>) {
       sandbox: async () => await resolveSandbox(),
     },
     async reaction => {
-      const [items, requirements, risks] = await Promise.all([
-        reaction.given(reaction.trigger).dataset({
+      const [repository, attachments] = await Promise.all([
+        reaction.given(reaction.trigger).git({
+          operation: "clone",
+          key: "reference",
+          url: repositoryURL(),
+          depth: 1,
+        }),
+        reaction.given(reaction.trigger).loadFiles(),
+      ])
+
+      const [items, analysis] = await Promise.all([
+        reaction.given(attachments).dataset({
           instruction: [
-            "Extract one row per requested item from the messageReceived Event.",
+            "Extract one row per requested item from the files materialized for this Event.",
             "Preserve quantity and unit exactly when stated.",
             "Do not invent products that are not present in the request.",
           ].join(" "),
           schema: workbenchItemSchema,
         }),
-        reaction.given(reaction.trigger).agent({
-          instruction: "Identify the request objective and every explicit commercial or delivery constraint.",
-          output: workbenchRequirementsSchema,
-        }),
-        reaction.given(reaction.trigger).agent({
-          instruction: "Assess concrete procurement risks in the request and classify each as low, medium, or high.",
-          output: workbenchRisksSchema,
+        reaction.given(attachments).agent({
+          instruction: [
+            "Identify the objective, explicit constraints, and concrete procurement risks.",
+            "Return no more than eight concise constraints and eight concise risks.",
+          ].join(" "),
+          output: workbenchAnalysisSchema,
         }),
       ])
 
-      const decision = await reaction.given([items, requirements, risks]).agent({
+      const decision = await reaction.given([repository, items, analysis]).agent({
+        path: repository.payload.path,
         instruction: [
-          "Produce a grounded decision from the formal item Dataset, requirements, and risks.",
+          "Produce a concise Markdown review from the formal item Dataset, analysis, and repository Event.",
           "The answer must mention the extracted item count and clearly state what should happen next.",
+          "Include the exact logical repository path and stay under 250 words.",
         ].join(" "),
         output: workbenchDecisionSchema,
       })
 
-      const datasetId = String((items.payload as any).datasetId)
-      const preview = Array.isArray((items.payload as any).preview)
-        ? (items.payload as any).preview
-        : []
-      const riskRows = Array.isArray((risks.payload as any).risks)
-        ? (risks.payload as any).risks
-        : []
+      const generated = await reaction.given(decision).shell({
+        command: "python3",
+        args: [
+          "-c",
+          [
+            "from pathlib import Path",
+            "import os",
+            "Path('workbench-review.md').write_text(os.environ['WORKBENCH_REPORT'], encoding='utf-8')",
+          ].join("; "),
+        ],
+        path: repository.payload.path,
+        env: { WORKBENCH_REPORT: decision.payload.answer },
+      })
+      if (!generated.payload.success) {
+        throw new Error(
+          `workbench_report_generation_failed:${generated.payload.error || generated.payload.output}`,
+        )
+      }
+
+      const published = await reaction.given(generated).storeFiles({
+        path: repository.payload.path,
+        files: "workbench-review.md",
+      })
+      const committed = await reaction.given(published).git({
+        operation: "commit",
+        path: repository.payload.path,
+        message: "Add generated workbench review",
+        authorName: "Ekairos Workbench",
+        authorEmail: "workbench@ekairos.dev",
+      })
+
+      const datasetId = items.payload.datasetId
+      const itemCount = items.payload.count ?? items.payload.preview.length
+      const riskRows = analysis.payload.risks
       const reviewId = uuidV5(`workbench:review:${datasetId}`)
-      const recorded = await reaction.given([items, risks, decision]).action(
+      const recorded = await reaction.given([
+        items,
+        analysis,
+        decision,
+        published,
+        committed,
+      ]).action(
         workbenchDomain.actions.recordReview,
         {
           reviewId,
           request: reaction.trigger.payload.text,
           items: { datasetId },
-          itemCount: preview.length,
+          itemCount,
           riskCount: riskRows.length,
           decision: decision.payload,
         },
@@ -118,9 +161,16 @@ function createReviewReaction(resolveSandbox: () => Promise<string>) {
           text: decision.payload.answer,
           reviewId,
           datasetId,
-          itemCount: preview.length,
+          itemCount,
           recommendation: decision.payload.recommendation,
-        }).link({ review: reviewId }),
+          artifactCount: published.payload.files.length,
+          ...(committed.payload.commitSha
+            ? { repositoryCommit: committed.payload.commitSha }
+            : {}),
+        }).link({
+          review: reviewId,
+          files: published.payload.files.map(file => file.fileId),
+        }),
       )
     },
   )
@@ -134,27 +184,27 @@ export async function answerMessage(input: {
   parts: readonly unknown[]
   scenario: WorkbenchScenario
 }) {
-  if (!input.context) throw new Error("workbench_context_not_found")
-  const messageBlocks = input.parts.length > 0
-    ? input.parts
-    : [{ type: "text", text: input.text }]
+  const context = input.context
+  if (!context) throw new Error("workbench_context_not_found")
+  const fileIds = await uploadInputFiles({ ...input, context })
+  const draft = workbenchDomain.events.messageReceived({
+    text: input.text,
+    scenario: input.scenario,
+  })
   const trigger = await Events(input.runtime).emit(
-    workbenchDomain.events.messageReceived({
-      text: input.text,
-      scenario: input.scenario,
-    }),
+    fileIds.length > 0 ? draft.link({ files: [...fileIds] }) : draft,
     {
       id: input.eventId,
       channel: "web",
-      contextId: input.context.id,
+      contextId: context.id,
       parts: [Part.message({
-        blocks: messageBlocks as any,
+        blocks: [{ type: "text", text: input.text }],
       })],
     },
   )
   const history = (await Events(input.runtime).query({
     $: {
-      where: { context: input.context.id, domain: "workbench" },
+      where: { context: context.id, domain: "workbench" },
       order: { createdAt: "asc" },
       limit: 200,
     },
@@ -167,8 +217,87 @@ export async function answerMessage(input: {
     : createChatReaction(history)
   return await executeReaction(
     input.runtime,
-    input.context,
+    context,
     trigger,
     definition,
   )
 }
+
+function repositoryURL() {
+  return String(
+    process.env.WORKBENCH_REPOSITORY_URL ??
+      "https://github.com/octocat/Hello-World.git",
+  ).trim()
+}
+
+async function uploadInputFiles(input: {
+  runtime: WorkbenchRuntime
+  context: ContextHandle<WorkbenchContext>
+  eventId: string
+  text: string
+  parts: readonly unknown[]
+  scenario: WorkbenchScenario
+}) {
+  const files: Array<{ name: string; mediaType: string; bytes: Buffer }> = []
+  for (const value of input.parts) {
+    if (!value || typeof value !== "object") continue
+    const part = value as Record<string, unknown>
+    if (part.type !== "file") continue
+    const url = typeof part.url === "string" ? part.url : ""
+    if (!url) continue
+    files.push({
+      name: safeFileName(
+        typeof part.filename === "string" ? part.filename : `attachment-${files.length + 1}`,
+      ),
+      mediaType: typeof part.mediaType === "string"
+        ? part.mediaType
+        : mediaTypeFromDataURL(url),
+      bytes: await readInputFile(url),
+    })
+  }
+  if (files.length === 0 && input.scenario === "review") {
+    files.push({
+      name: "request.md",
+      mediaType: "text/markdown",
+      bytes: Buffer.from(input.text, "utf8"),
+    })
+  }
+
+  const db = await input.runtime.db()
+  const ids: string[] = []
+  for (const [index, file] of files.entries()) {
+    const uploaded = await db.storage.uploadFile(
+      `/workbench/${input.context.id}/incoming/${input.eventId}/${index}-${file.name}`,
+      file.bytes,
+      { contentType: file.mediaType, contentDisposition: file.name },
+    )
+    const fileId = String((uploaded as any)?.data?.id ?? "")
+    if (!fileId) throw new Error(`workbench_file_upload_failed:${file.name}`)
+    ids.push(fileId)
+  }
+  return Object.freeze(ids)
+}
+
+async function readInputFile(url: string) {
+  if (url.startsWith("data:")) {
+    const comma = url.indexOf(",")
+    if (comma < 0) throw new Error("workbench_file_data_url_invalid")
+    const metadata = url.slice(5, comma)
+    const data = url.slice(comma + 1)
+    return metadata.includes(";base64")
+      ? Buffer.from(data, "base64")
+      : Buffer.from(decodeURIComponent(data), "utf8")
+  }
+  const response = await fetch(url)
+  if (!response.ok) throw new Error(`workbench_file_fetch_failed:${response.status}`)
+  return Buffer.from(await response.arrayBuffer())
+}
+
+function mediaTypeFromDataURL(url: string) {
+  return /^data:([^;,]+)/.exec(url)?.[1] ?? "application/octet-stream"
+}
+
+function safeFileName(value: string) {
+  const name = value.replace(/\\/g, "/").split("/").pop() ?? "file"
+  return name.replace(/[^a-zA-Z0-9._-]+/g, "-") || "file"
+}
diff --git a/packages/workbench-v3/src/components/workbench.tsx b/packages/workbench-v3/src/components/workbench.tsx
index 2fe39db..b266de8 100644
--- a/packages/workbench-v3/src/components/workbench.tsx
+++ b/packages/workbench-v3/src/components/workbench.tsx
@@ -21,18 +21,21 @@ import {
   LoaderCircle,
   MessageSquare,
   Network,
+  Paperclip,
   Radio,
   TerminalSquare,
   User,
+  X,
+  ZoomIn,
+  ZoomOut,
 } from "lucide-react"
 import { FormEvent, useEffect, useMemo, useRef, useState } from "react"
 import { Streamdown } from "streamdown"
 
 import { workbenchClientSchema } from "@/src/client-schema"
 import {
-  buildReactionStages,
+  buildEventGraph,
   reactionDurationMs,
-  reactionEventTypes,
   reactionTitle,
 } from "@/src/reaction-graph"
 
@@ -130,6 +133,7 @@ function PartView({ part }: { part: ContextEventPartForUI }) {
 function Message({ event }: { event: ContextEventForUI }) {
   const user = event.type === "workbench.messageReceived" || event.type === "user.message"
   const text = eventText(event)
+  const files = linkedFileCount(event)
   if (!text && event.eventParts.length === 0) return null
   return (
     <article className={`message ${user ? "message-user" : "message-assistant"}`}>
@@ -137,6 +141,7 @@ function Message({ event }: { event: ContextEventForUI }) {
         <span className="message-author">{user ? <User size={14} /> : <Bot size={14} />}</span>
         <strong>{user ? "You" : "Reaction"}</strong>
         <span>{time(event.createdAt)}</span>
+        {files > 0 ? <span className="message-files"><Paperclip size={11} />{files}</span> : null}
         <span className={`durability durability-${event.durability ?? "durable"}`}>
           {event.durability ?? "durable"}
         </span>
@@ -148,88 +153,144 @@ function Message({ event }: { event: ContextEventForUI }) {
   )
 }
 
-function EventTypes({ values }: { values: readonly string[] }) {
-  if (values.length === 0) return <span className="event-type empty-event">pending</span>
-  return <>{values.map(value => <span className="event-type" key={value}>{value}</span>)}</>
-}
-
-function ReactionCard({
-  reaction,
-  selected,
-  onSelect,
-}: {
-  reaction: ContextReactionForUI
-  selected: boolean
-  onSelect(): void
-}) {
-  const duration = reactionDurationMs(reaction)
-  return (
-    <button className={`causal-reaction ${selected ? "selected" : ""}`} onClick={onSelect}>
-      <span className="causal-events"><EventTypes values={reactionEventTypes(reaction, "causes")} /></span>
-      <span className="causal-edge">
-        <span className="operation-badge">{reaction.type}</span>
-        <strong>{reactionTitle(reaction)}</strong>
-      </span>
-      <span className="causal-events effects"><EventTypes values={reactionEventTypes(reaction, "effects")} /></span>
-      <span className="causal-state">
-        <StatusDot status={reaction.status} />
-        <span>{reaction.stream?.reader.chunkCount ?? 0} chunks</span>
-        <span>{duration === null ? "-" : `${(duration / 1000).toFixed(1)}s`}</span>
-      </span>
-    </button>
-  )
-}
-
-function SessionFlow({
+function EventGraphView({
   session,
   selectedId,
   onSelect,
-  nested = false,
 }: {
   session: ContextSessionForUI
   selectedId: string | null
   onSelect(id: string): void
-  nested?: boolean
 }) {
-  const stages = buildReactionStages(session)
+  const graph = useMemo(() => buildEventGraph(session), [session])
+  const [scale, setScale] = useState(0.82)
+  const producers = useMemo(() => new Map(
+    graph.reactions.flatMap(edge =>
+      edge.reaction.effectIds.map(eventId => [eventId, edge.reaction.id] as const)),
+  ), [graph])
   return (
-    <section className={`session-flow ${nested ? "nested" : ""}`}>
-      <header className="session-flow-header">
-        <span>{nested ? "Child Session" : "Reaction"}</span>
-        <strong>{session.definition}</strong>
-        <small>{session.status}</small>
+    <section className="event-graph">
+      <header className="event-graph-toolbar">
+        <div>
+          <strong>{session.definition}</strong>
+          <span>{graph.nodes.length} Events / {graph.reactions.length} Reactions</span>
+        </div>
+        <div className="graph-zoom">
+          <button aria-label="Zoom out" onClick={() => setScale(value => Math.max(0.5, value - 0.1))}>
+            <ZoomOut size={13} />
+          </button>
+          <span>{Math.round(scale * 100)}%</span>
+          <button aria-label="Zoom in" onClick={() => setScale(value => Math.min(1.2, value + 0.1))}>
+            <ZoomIn size={13} />
+          </button>
+        </div>
       </header>
-      {stages.map((stage, index) => (
-        <div className="causal-stage" key={`${session.id}:${stage.rank}`}>
-          <div className="stage-label">
-            <span>stage {index + 1}</span>
-            {stage.parallel ? <strong><GitFork size={11} />parallel fan-out</strong> : null}
-          </div>
-          <div className={`stage-reactions ${stage.parallel ? "parallel" : ""}`}>
-            {stage.reactions.map(reaction => (
-              <ReactionCard
-                key={reaction.id}
-                reaction={reaction}
-                selected={reaction.id === selectedId}
-                onSelect={() => onSelect(reaction.id)}
-              />
-            ))}
+      <div className="event-graph-viewport">
+        <div
+          className="event-graph-scaled"
+          style={{ width: graph.width * scale, height: graph.height * scale }}
+        >
+          <div
+            className="event-graph-canvas"
+            style={{
+              width: graph.width,
+              height: graph.height,
+              transform: `scale(${scale})`,
+            }}
+          >
+            <svg aria-hidden="true" height={graph.height} width={graph.width}>
+              <defs>
+                <marker id="graph-arrow" markerHeight="6" markerWidth="6" orient="auto" refX="5" refY="3">
+                  <path d="M0,0 L6,3 L0,6 Z" />
+                </marker>
+              </defs>
+              {graph.connectors.map(connector => {
+                const middleY = (connector.fromY + connector.toY) / 2
+                return (
+                  <path
+                    className={connector.reactionId === selectedId ? "selected" : ""}
+                    d={`M ${connector.fromX} ${connector.fromY} C ${connector.fromX} ${middleY}, ${connector.toX} ${middleY}, ${connector.toX} ${connector.toY}`}
+                    key={connector.id}
+                    markerEnd="url(#graph-arrow)"
+                  />
+                )
+              })}
+            </svg>
+            {graph.nodes.map(node => {
+              const fileCount = linkedFileCount(node.event)
+              const producer = producers.get(node.id)
+              return (
+                <button
+                  className={`event-node durability-${node.event.durability ?? "durable"}`}
+                  data-event-id={node.id}
+                  key={node.id}
+                  onClick={() => producer && onSelect(producer)}
+                  style={{
+                    left: node.x,
+                    top: node.y,
+                    width: node.width,
+                    height: node.height,
+                  }}
+                  title={node.id}
+                >
+                  <span className="event-node-type">{node.event.type}</span>
+                  <strong>{eventSummary(node.event)}</strong>
+                  <span className="event-node-meta">
+                    <code>{short(node.id, 10)}</code>
+                    <span>{node.event.eventParts.length} parts</span>
+                    {fileCount > 0 ? <span>{fileCount} files</span> : null}
+                  </span>
+                </button>
+              )
+            })}
+            {graph.reactions.map(edge => {
+              const duration = reactionDurationMs(edge.reaction)
+              return (
+                <button
+                  className={`reaction-edge reaction-${edge.reaction.status} ${edge.id === selectedId ? "selected" : ""}`}
+                  key={edge.id}
+                  onClick={() => onSelect(edge.id)}
+                  style={{
+                    left: edge.x,
+                    top: edge.y,
+                    width: edge.width,
+                    height: edge.height,
+                  }}
+                  title={reactionTitle(edge.reaction)}
+                >
+                  <StatusDot status={edge.reaction.status} />
+                  <strong>{edge.reaction.type}</strong>
+                  <span>{duration === null ? "-" : `${(duration / 1000).toFixed(1)}s`}</span>
+                </button>
+              )
+            })}
           </div>
         </div>
-      ))}
-      {session.children.map(child => (
-        <SessionFlow
-          key={child.id}
-          session={child}
-          selectedId={selectedId}
-          onSelect={onSelect}
-          nested
-        />
-      ))}
+      </div>
     </section>
   )
 }
 
+function linkedFileCount(event: ContextEventForUI) {
+  const value = event.links.files
+  if (Array.isArray(value)) return value.length
+  return typeof value === "string" && value ? 1 : 0
+}
+
+function eventSummary(event: ContextEventForUI) {
+  const payload = event.payload as any
+  const text = typeof payload === "string"
+    ? payload
+    : typeof payload?.text === "string"
+      ? payload.text
+      : typeof payload?.summary === "string"
+        ? payload.summary
+        : ""
+  if (text) return text.length > 62 ? `${text.slice(0, 59)}...` : text
+  const type = event.type.split(".").pop() ?? event.type
+  return type.replace(/([a-z])([A-Z])/g, "$1 $2")
+}
+
 function ChunkTrace({ reaction }: { reaction: ContextReactionForUI | null }) {
   if (!reaction) return <div className="empty"><GitFork size={20} />Select a Reaction.</div>
   const stream = reaction.stream
@@ -319,6 +380,8 @@ function LiveWorkbench({ bootstrap }: { bootstrap: Bootstrap }) {
     streamReactionIds,
   })
   const [draft, setDraft] = useState(REVIEW_SAMPLE)
+  const [attachment, setAttachment] = useState<File | null>(null)
+  const fileInput = useRef<HTMLInputElement>(null)
   const bottom = useRef<HTMLDivElement>(null)
   const chatEvents = useMemo(() => context.events.filter(event =>
     event.type === "workbench.messageReceived" ||
@@ -354,8 +417,18 @@ function LiveWorkbench({ bootstrap }: { bootstrap: Bootstrap }) {
     event.preventDefault()
     const value = draft.trim()
     if (!value || context.sendStatus !== "idle") return
+    const parts: any[] = [{ type: "text", text: value }]
+    if (attachment) {
+      parts.push({
+        type: "file",
+        filename: attachment.name,
+        mediaType: attachment.type || "application/octet-stream",
+        url: await fileDataURL(attachment),
+      })
+    }
     setDraft("")
-    await context.append({ parts: [{ type: "text", text: value }] }).catch(() => undefined)
+    setAttachment(null)
+    await context.append({ parts }).catch(() => undefined)
   }
 
   return (
@@ -443,8 +516,32 @@ function LiveWorkbench({ bootstrap }: { bootstrap: Bootstrap }) {
                   Load sample
                 </button>
               ) : null}
+              {attachment ? (
+                <span className="attachment-chip">
+                  <Paperclip size={11} />
+                  <span>{attachment.name}</span>
+                  <button aria-label="Remove attachment" onClick={() => setAttachment(null)} type="button">
+                    <X size={11} />
+                  </button>
+                </span>
+              ) : null}
             </div>
             <form className="composer" onSubmit={submit}>
+              <input
+                accept=".csv,.json,.md,.pdf,.png,.jpg,.jpeg,.txt"
+                hidden
+                onChange={event => setAttachment(event.target.files?.[0] ?? null)}
+                ref={fileInput}
+                type="file"
+              />
+              <button
+                aria-label="Attach file"
+                className="attach-button"
+                onClick={() => fileInput.current?.click()}
+                type="button"
+              >
+                <Paperclip size={16} />
+              </button>
               <textarea
                 aria-label="Message"
                 placeholder={scenario === "review" ? "Describe a request with concrete items..." : "Ask something that produces a trace..."}
@@ -457,7 +554,11 @@ function LiveWorkbench({ bootstrap }: { bootstrap: Bootstrap }) {
                   }
                 }}
               />
-              <button aria-label="Send message" disabled={!draft.trim() || context.sendStatus !== "idle"}>
+              <button
+                aria-label="Send message"
+                className="send-button"
+                disabled={!draft.trim() || context.sendStatus !== "idle"}
+              >
                 {context.sendStatus === "idle" ? <ArrowUp size={18} /> : <LoaderCircle className="spin" size={18} />}
               </button>
             </form>
@@ -468,7 +569,7 @@ function LiveWorkbench({ bootstrap }: { bootstrap: Bootstrap }) {
           <div className="panel-title"><span>Causal graph</span><GitFork size={15} /></div>
           <div className="reaction-flow">
             {activeSession ? (
-              <SessionFlow
+              <EventGraphView
                 session={activeSession}
                 selectedId={selected?.id ?? null}
                 onSelect={setSelectedId}
@@ -504,3 +605,12 @@ export function Workbench() {
 function errorText(value: unknown) {
   return value instanceof Error ? value.message : String(value)
 }
+
+function fileDataURL(file: File) {
+  return new Promise<string>((resolve, reject) => {
+    const reader = new FileReader()
+    reader.onerror = () => reject(reader.error ?? new Error("file_read_failed"))
+    reader.onload = () => resolve(String(reader.result ?? ""))
+    reader.readAsDataURL(file)
+  })
+}
diff --git a/packages/workbench-v3/src/domain.ts b/packages/workbench-v3/src/domain.ts
index 469be8a..c4b1995 100644
--- a/packages/workbench-v3/src/domain.ts
+++ b/packages/workbench-v3/src/domain.ts
@@ -18,22 +18,19 @@ export const workbenchItemSchema = z.object({
   unit: z.string().min(1),
 })
 
-export const workbenchRequirementsSchema = z.object({
-  objective: z.string().min(1),
-  constraints: z.array(z.string()),
-})
-
-export const workbenchRisksSchema = z.object({
+export const workbenchAnalysisSchema = z.object({
+  objective: z.string().min(1).max(500),
+  constraints: z.array(z.string().min(1).max(300)).max(8),
   risks: z.array(z.object({
-    risk: z.string().min(1),
+    risk: z.string().min(1).max(500),
     severity: z.enum(["low", "medium", "high"]),
-  })),
+  })).max(8),
 })
 
 export const workbenchDecisionSchema = z.object({
   recommendation: z.enum(["proceed", "clarify", "reject"]),
-  summary: z.string().min(1),
-  answer: z.string().min(1),
+  summary: z.string().min(1).max(500),
+  answer: z.string().min(1).max(3_000),
 })
 
 const workbenchCore = domain("workbench")
@@ -68,6 +65,9 @@ const workbenchCore = domain("workbench")
         text: z.string().min(1),
         scenario: workbenchScenarioSchema,
       }),
+      links: {
+        files: { on: "$files", has: "many" },
+      },
     }),
     messageAnswered: defineEvent({
       payload: z.object({ text: z.string().min(1) }),
@@ -79,9 +79,12 @@ const workbenchCore = domain("workbench")
         datasetId: z.string().min(1),
         itemCount: z.number().int().nonnegative(),
         recommendation: z.enum(["proceed", "clarify", "reject"]),
+        artifactCount: z.number().int().nonnegative(),
+        repositoryCommit: z.string().optional(),
       }),
       links: {
         review: { on: "workbench_reviews", has: "one" },
+        files: { on: "$files", has: "many" },
       },
     }),
   })
diff --git a/packages/workbench-v3/src/reaction-graph.test.ts b/packages/workbench-v3/src/reaction-graph.test.ts
index f9e14d9..e80bdb9 100644
--- a/packages/workbench-v3/src/reaction-graph.test.ts
+++ b/packages/workbench-v3/src/reaction-graph.test.ts
@@ -6,6 +6,7 @@ import type {
 import { describe, expect, it } from "vitest"
 
 import {
+  buildEventGraph,
   buildReactionStages,
   reactionTitle,
 } from "./reaction-graph"
@@ -89,5 +90,27 @@ describe("Workbench causal graph", () => {
     expect(stages[0]?.parallel).toBe(true)
     expect(stages.slice(1).every(stage => !stage.parallel)).toBe(true)
     expect(reactionTitle(rows[6]!)).toBe("Publish reviewCompleted")
+
+    const graph = buildEventGraph(session)
+    expect(graph.nodes.map(node => [node.id, node.rank])).toEqual([
+      ["trigger", 0],
+      ["dataset", 1],
+      ["requirements", 1],
+      ["risks", 1],
+      ["decision", 2],
+      ["recorded", 3],
+      ["completed", 4],
+    ])
+    expect(graph.reactions.map(edge => edge.id)).toEqual([
+      "items",
+      "requirements",
+      "risks",
+      "decision",
+      "record",
+      "emit",
+    ])
+    expect(graph.connectors.filter(edge => edge.reactionId === "decision")).toHaveLength(4)
+    expect(graph.width).toBeGreaterThan(600)
+    expect(graph.height).toBeGreaterThan(600)
   })
 })
diff --git a/packages/workbench-v3/src/reaction-graph.ts b/packages/workbench-v3/src/reaction-graph.ts
index 9b2addd..63c61cc 100644
--- a/packages/workbench-v3/src/reaction-graph.ts
+++ b/packages/workbench-v3/src/reaction-graph.ts
@@ -10,6 +10,169 @@ export type ReactionStage = Readonly<{
   parallel: boolean
 }>
 
+export type EventGraphNode = Readonly<{
+  id: string
+  event: ContextEventForUI
+  rank: number
+  x: number
+  y: number
+  width: number
+  height: number
+}>
+
+export type EventGraphReaction = Readonly<{
+  id: string
+  reaction: ContextReactionForUI
+  x: number
+  y: number
+  width: number
+  height: number
+}>
+
+export type EventGraphConnector = Readonly<{
+  id: string
+  reactionId: string
+  fromX: number
+  fromY: number
+  toX: number
+  toY: number
+}>
+
+export type EventGraph = Readonly<{
+  width: number
+  height: number
+  nodes: readonly EventGraphNode[]
+  reactions: readonly EventGraphReaction[]
+  connectors: readonly EventGraphConnector[]
+}>
+
+const EVENT_WIDTH = 184
+const EVENT_HEIGHT = 78
+const REACTION_WIDTH = 122
+const REACTION_HEIGHT = 26
+const COLUMN_GAP = 28
+const RANK_GAP = 92
+const GRAPH_PADDING = 24
+
+export function buildEventGraph(session: ContextSessionForUI): EventGraph {
+  const rootId = session.rootReaction?.id
+  const reactions = session.reactions
+    .filter(reaction => reaction.id !== rootId)
+    .sort((left, right) => left.position - right.position)
+  const events = new Map<string, ContextEventForUI>()
+  if (session.trigger) events.set(session.trigger.id, session.trigger)
+  for (const reaction of reactions) {
+    reaction.causes.forEach(event => events.set(event.id, event))
+    reaction.effects.forEach(event => events.set(event.id, event))
+    if (reaction.liveEffect) events.set(reaction.liveEffect.id, reaction.liveEffect)
+  }
+
+  const ranks = new Map<string, number>()
+  if (session.trigger) ranks.set(session.trigger.id, 0)
+  for (const event of events.values()) {
+    if (!ranks.has(event.id)) ranks.set(event.id, 0)
+  }
+  for (let pass = 0; pass <= reactions.length; pass += 1) {
+    let changed = false
+    for (const reaction of reactions) {
+      const causeRank = Math.max(0, ...reaction.causeIds.map(id => ranks.get(id) ?? 0))
+      const nextRank = causeRank + 1
+      const effects = reaction.effects.length > 0
+        ? reaction.effects
+        : reaction.liveEffect ? [reaction.liveEffect] : []
+      for (const effect of effects) {
+        if ((ranks.get(effect.id) ?? -1) >= nextRank) continue
+        ranks.set(effect.id, nextRank)
+        changed = true
+      }
+    }
+    if (!changed) break
+  }
+
+  const grouped = new Map<number, ContextEventForUI[]>()
+  for (const event of events.values()) {
+    const rank = ranks.get(event.id) ?? 0
+    grouped.set(rank, [...(grouped.get(rank) ?? []), event])
+  }
+  const rows = [...grouped.entries()]
+    .sort(([left], [right]) => left - right)
+    .map(([rank, values]) => [rank, values.sort(eventOrder)] as const)
+  const widest = Math.max(1, ...rows.map(([, values]) => values.length))
+  const width = Math.max(
+    420,
+    GRAPH_PADDING * 2 + widest * EVENT_WIDTH + (widest - 1) * COLUMN_GAP,
+  )
+  const nodes: EventGraphNode[] = []
+  for (const [rank, values] of rows) {
+    const rowWidth = values.length * EVENT_WIDTH + (values.length - 1) * COLUMN_GAP
+    const startX = (width - rowWidth) / 2
+    values.forEach((event, index) => nodes.push(Object.freeze({
+      id: event.id,
+      event,
+      rank,
+      x: startX + index * (EVENT_WIDTH + COLUMN_GAP),
+      y: GRAPH_PADDING + rank * (EVENT_HEIGHT + RANK_GAP),
+      width: EVENT_WIDTH,
+      height: EVENT_HEIGHT,
+    })))
+  }
+  const byId = new Map(nodes.map(node => [node.id, node]))
+  const reactionLayouts: EventGraphReaction[] = []
+  const connectors: EventGraphConnector[] = []
+  for (const reaction of reactions) {
+    const sources = reaction.causeIds
+      .map(id => byId.get(id))
+      .filter((value): value is EventGraphNode => Boolean(value))
+    const effectEvents = reaction.effects.length > 0
+      ? reaction.effects
+      : reaction.liveEffect ? [reaction.liveEffect] : []
+    const targets = effectEvents
+      .map(event => byId.get(event.id))
+      .filter((value): value is EventGraphNode => Boolean(value))
+    if (sources.length === 0 || targets.length === 0) continue
+    const centerX = average([
+      ...sources.map(node => node.x + node.width / 2),
+      ...targets.map(node => node.x + node.width / 2),
+    ])
+    const sourceBottom = Math.max(...sources.map(node => node.y + node.height))
+    const targetTop = Math.min(...targets.map(node => node.y))
+    const centerY = (sourceBottom + targetTop) / 2
+    const layout = Object.freeze({
+      id: reaction.id,
+      reaction,
+      x: centerX - REACTION_WIDTH / 2,
+      y: centerY - REACTION_HEIGHT / 2,
+      width: REACTION_WIDTH,
+      height: REACTION_HEIGHT,
+    })
+    reactionLayouts.push(layout)
+    sources.forEach((source, index) => connectors.push(Object.freeze({
+      id: `${reaction.id}:cause:${source.id}:${index}`,
+      reactionId: reaction.id,
+      fromX: source.x + source.width / 2,
+      fromY: source.y + source.height,
+      toX: centerX,
+      toY: layout.y,
+    })))
+    targets.forEach((target, index) => connectors.push(Object.freeze({
+      id: `${reaction.id}:effect:${target.id}:${index}`,
+      reactionId: reaction.id,
+      fromX: centerX,
+      fromY: layout.y + layout.height,
+      toX: target.x + target.width / 2,
+      toY: target.y,
+    })))
+  }
+  const maxRank = Math.max(0, ...nodes.map(node => node.rank))
+  return Object.freeze({
+    width,
+    height: GRAPH_PADDING * 2 + (maxRank + 1) * EVENT_HEIGHT + maxRank * RANK_GAP,
+    nodes: Object.freeze(nodes),
+    reactions: Object.freeze(reactionLayouts),
+    connectors: Object.freeze(connectors),
+  })
+}
+
 export function buildReactionStages(session: ContextSessionForUI): readonly ReactionStage[] {
   const rootId = session.rootReaction?.id
   const reactions = session.reactions
@@ -101,3 +264,12 @@ function firstSentence(value: string | undefined) {
 function unique(values: readonly string[]) {
   return [...new Set(values)]
 }
+
+function average(values: readonly number[]) {
+  return values.reduce((sum, value) => sum + value, 0) / Math.max(values.length, 1)
+}
+
+function eventOrder(left: ContextEventForUI, right: ContextEventForUI) {
+  const created = new Date(left.createdAt).valueOf() - new Date(right.createdAt).valueOf()
+  return created || left.id.localeCompare(right.id)
+}
